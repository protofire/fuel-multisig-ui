import { Box } from "@mui/material";

export const AssetsTable: React.FC = () => {
  //   const [type, setType] = useState(types[0]);
  //   const [open, setOpen] = useState(false);

  return (
    <Box sx={{ width: "100%" }}>
      {/* <AssetTabs options={["Tokens"]}></AssetTabs> */}
    </Box>
  );
};
