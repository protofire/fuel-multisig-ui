import { Box } from "@mui/material";

export function SetUpMultisigStepper() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "space-between",
        flexDirection: "column",
        margin: "4rem",
      }}
    ></Box>
  );
}
